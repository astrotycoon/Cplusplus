#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
	const int ival = 512, jval = 1024, kval = 4096;
	int buf_size;
	scanf("%d", &buf_size);
	
	switch(buf_size) {
	case ival:
		break;
	case jval:
		break;
	case kval:
		break;
	}

	return 0;
}